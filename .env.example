# Shared
KAFKA_BOOTSTRAP=kafka:9092
JWT_ISSUER=paytm-style
JWT_SECRET=dev-secret-change
JWT_TTL_SECONDS=3600
INTERNAL_JWT_TTL_SECONDS=300

# Ledger DB
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_HOST=mysql
MYSQL_DB=ledger
MYSQL_PORT=3306

# Redis (notifications dedupe & idempotency sample)
REDIS_URL=redis://redis:6379/0

# Cassandra
CASSANDRA_HOST=cassandra
CASSANDRA_KEYSPACE=paytm_style
